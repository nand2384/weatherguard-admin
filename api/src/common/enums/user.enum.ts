export enum UserRole {
  USER = 'USER',
  ADMIN = 'ADMIN',
}

export enum ApprovalStatus {
  PENDING = 'PENDING',
  APPROVED = 'APPROVED',
  REJECTED = 'REJECTED',
}

export enum AuthProvider {
  GOOGLE = 'google',
  GITHUB = 'github',
}